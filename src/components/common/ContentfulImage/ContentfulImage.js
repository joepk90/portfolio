import ContentfulImage from '@moxy/react-contentful-image';

// TODO Setup as default export called ContentfulImage
export const ContentfulImageAlias = ({ image }) => {
    return <ContentfulImage image={image} />
}